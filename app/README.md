# 前端（app/）

網頁／App **只做這個資料夾**。量測 Python 在上一層，請不要把前端檔案散到倉庫根目錄。

```bash
cd app
npm create vite@latest . -- --template react
```

開發先讀 [`example-data/park_inventory_report.sample.json`](example-data/park_inventory_report.sample.json)。畫面、燈號、欄位說明以倉庫根目錄 [`README.md`](../README.md) 第 8 節為準。
